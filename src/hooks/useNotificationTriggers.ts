import { useEffect } from 'react';

// Placeholder hook: notification triggers were removed in pivot.
export function useNotificationTriggers() {
  useEffect(() => {
    // no-op placeholder for removed notification triggers
  }, []);
}

export default useNotificationTriggers;
